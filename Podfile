# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'CHContainer' do
  inhibit_all_warnings!
  use_frameworks!
  
  # Pods for CHContainerView
  pod 'SnapKit' 

end
